int printInt(int i);

int add(int x, int y) {
  var z = x + y;
  return z;
}

int main() {
  var z = add;
  var i = 10;
  var sum = 0;
  while (i > 0) {
    sum = sum + map(z, 1, 2);
    i = i - 1;
    var q = printInt(sum);
  }
  return sum;
}

int map ((int, int) -> int f, int x, int y) {
  return f(x, y);
}